name: Security Review

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

jobs:
  semgrep:
    name: SAST (Semgrep)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Semgrep (auto ruleset)
        run: |
          mkdir -p artifacts
          docker run --rm \
            -v "${PWD}:/src" \
            -w /src \
            semgrep/semgrep:latest \
            semgrep scan --config auto --error --json --output /src/artifacts/semgrep.json
      - name: Upload Semgrep report
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: artifacts/semgrep.json
          if-no-files-found: warn

  secrets:
    name: Secret Scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run gitleaks
        run: |
          mkdir -p artifacts
          docker run --rm \
            -v "${PWD}:/repo" \
            zricethezav/gitleaks:latest \
            detect --source /repo --config /repo/.gitleaks.toml \
            --report-format json --report-path /repo/artifacts/gitleaks.json
      - name: Upload gitleaks report
        uses: actions/upload-artifact@v4
        with:
          name: gitleaks-report
          path: artifacts/gitleaks.json
          if-no-files-found: warn

  dependency-review:
    name: Dependency Review (PR)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: actions/dependency-review-action
        uses: actions/dependency-review-action@v4

  osv:
    name: OSV scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run osv-scanner
        run: |
          mkdir -p artifacts
          docker run --rm \
            -v "${PWD}:/src" \
            -w /src \
            ghcr.io/google/osv-scanner:latest \
            scan source -r . --format json --output-file /src/artifacts/osv.json
      - name: Upload OSV report
        uses: actions/upload-artifact@v4
        with:
          name: osv-report
          path: artifacts/osv.json
          if-no-files-found: warn

  pip-audit:
    name: pip-audit (backend/admin/mock-ggg)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: [backend, admin, mock-ggg]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
      - name: pip-audit
        working-directory: ${{ matrix.project }}
        run: |
          uv sync --frozen || uv sync
          uv export --frozen --format requirements.txt --no-emit-project --output-file /tmp/requirements-audit.txt
          uv run pip-audit --strict -r /tmp/requirements-audit.txt

  npm-audit:
    name: npm audit (frontend)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: npm audit
        env:
          NPM_CONFIG_UPDATE_NOTIFIER: "false"
        run: npm audit --omit=dev --audit-level=high
